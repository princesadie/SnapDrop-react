'use strict'
const React = require('react-native');
const {
  StyleSheet,
  PixelRatio,
} = React;

module.exports = StyleSheet.create({
  imageContainer: {
    flex: 1,
    alignItems: 'stretch'
  },
  image: {
    flex: 1
  }
});
